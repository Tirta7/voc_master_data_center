# Mosquitto MQTT Broker Configuration
# Save this as mosquitto.conf and run: mosquitto -c mosquitto.conf

# ---- TCP Listener (for backend NestJS) ----
listener 1883
allow_anonymous true

# ---- WebSocket Listener (for frontend browser) ----
listener 8083
protocol websockets
allow_anonymous true
